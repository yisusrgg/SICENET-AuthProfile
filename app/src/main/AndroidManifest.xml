<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="com.example.sicenet_authprofile.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.example.sicenet_authprofile.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:name=".SicenetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SICENETAuthProfile"
        android:usesCleartextTraffic="true">

        <provider
            android:name=".data.provider.SicenetContentProvider"
            android:authorities="com.example.sicenet_authprofile.provider"
            android:exported="true"
            android:readPermission="com.example.sicenet_authprofile.READ_DATABASE"
            android:writePermission="com.example.sicenet_authprofile.WRITE_DATABASE" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SICENETAuthProfile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>